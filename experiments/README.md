# Experiments

Modal experiment scripts used for zero-shot baselines, frame-rate sweeps, and preprocessing comparisons.

- `inference/`: LTX-2B/LTX-13B generation experiments and baseline sweeps.
- `comparisons/`: direct visual comparisons such as RIFE vs FFmpeg minterpolate.

These scripts are intentionally preserved because they document the experiments already run.
